import sys
input = sys.stdin.readline


def solution(n, build_frame):
    # 0 : 설치X
    # 1 : 기둥
    # 2 : 보
    board = [[False for _ in range(2*n)] for _ in range(2*n)]

    for i in range(len(build_frame)):
        y,x,a,b = build_frame[i]
        

    answer = [[]]


    return answer